version: '3.8'

services:
  eisr_portal:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: eisr_production_portal
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
